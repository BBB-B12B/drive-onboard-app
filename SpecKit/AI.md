# SpecKit System: AI Collaboration Protocol

เอกสารนี้คือ **"ระบบปฏิบัติการ" (Operating System)** สำหรับการทำงานร่วมกันระหว่าง Human & AI เพื่อให้ทุก Project มีโครงสร้างที่เป็นระเบียบ ตรวจสอบได้ และสามารถส่งงานต่อ (Handoff) ได้อย่างลื่นไหลไร้รอยต่อ

---

## 🗣️ Language Guideline (ข้อกำหนดด้านภาษา)
> **Requirement**: ในการสร้าง หรือแก้ไขเอกสาร `SpecKit` และ Artifacts ต่างๆ ให้ใช้รูปแบบ **Mixed English-Thai**
> *   **Technical Terms**: ใช้ภาษาอังกฤษ (e.g., `Infrastructure`, `Scope`, `Implementation Details`)
> *   **Explanation**: ใช้ภาษาไทยอธิบายขยายความ เพื่อให้ทีมพัฒนาเข้าใจได้รวดเร็วและชัดเจนที่สุด (e.g., "ตรวจสอบโครงสร้างระบบ", "เข้าใจภาพรวมและขอบเขต")

## 📂 Phase 1: Structure Setup
ทุกโปรเจกต์ต้องมีโฟลเดอร์ `SpecKit/` ที่ประกอบด้วย 4 ไฟล์หลักเปรียบเสมือน "สมอง" ของโปรเจกต์:

### 1. `SpecKit/spec.md` (The "What")
> **จุดประสงค์**: ระบุ Business Logic และ Requirements
> **สิ่งที่ต้องมี**:
> - **[F-XXX] System Features**: ตารางฟีเจอร์ทั้งหมด พร้อม ID กำกับ
> - **Data Models**: โครงสร้างข้อมูล (Database Schema / ER Diagram)
> - **User Flows**: ขั้นตอนการทำงานของผู้ใช้ (Step-by-step)
> - **System Sitemap**: แผนผังหน้าจอทั้งหมด (Mermaid Diagram)

### 2. `SpecKit/instruction.md` (The "How")
> **จุดประสงค์**: ระบุ Tech Stack และมาตรฐานการเขียน Code
> **สิ่งที่ต้องมี**:
> - **Tech Stack**: ภาษา, Framework, Database, และ Libraries หลัก
> - **Project Structure**: ผังโครงสร้างไฟล์และโฟลเดอร์ (Tree view)
> - **Conventions**: กฎการตั้งชื่อ, Pattern การจัดการ State, สิ่งที่ห้ามทำ (Do's & Don'ts)
> - **Active Constraints / Known Issues**: ข้อจำกัดหรือ Tech Debt ที่เรารู้แต่ยอมปล่อยผ่านชั่วคราว (เพื่อไม่ให้ AI ตัวถัดไปพยายามแก้สิ่งที่ยังไม่ควรแก้)

### 3. `SpecKit/task.md` (The "When")
> **จุดประสงค์**: แผนงานและสถานะปัจจุบัน (Single Source of Truth for Progress)
> **สิ่งที่ต้องมี**:
> - แบ่งงานเป็น **Phases**
> - ใช้ **Rich Task Components** สำหรับงานซับซ้อน:
>    - **[T-XXX] Task Name**
>        - **Concept/Goal**: เป้าหมายคืออะไร
>        - **Implementation Details**: แนวทาง Technical (UI, Logic, Data)
>        - **Decision Log**: บันทึกเหตุผลสำคัญ (Why) ว่าทำไมถึงเลือกท่านี้ (เผื่อคนมาอ่านทีหลังสงสัย)
>        - **Verification Steps**: ขั้นตอนการเทส (Manual test ขั้นที่ 1-2-3 หรือ Command ที่ต้องรัน)
>        - **Sub-tasks**: งานย่อยๆ ที่ต้องทำ

### 4. `SpecKit/traceability.md` (The "Where")
> **จุดประสงค์**: แผนที่นำทาง (Map) เชื่อมโยง Requirement กับ Code
> **สิ่งที่ต้องมี**:
> 1. **Feature Matrix**: ตาราง Map ระหว่าง `Feature ID` <-> `Task ID` <-> `File Paths`
> 2. **Data/Variable Traceability Matrix (สำคัญมาก)**: ตาราง Map ระหว่าง Entity และตัวแปรใน Code เพื่อให้ Debug ง่าย
>
> **Template ตราง Data/Variable Traceability:**
> | ข้อมูลหลัก (Entity) | ชื่อ Interface/Type | ตัวแปร State หลัก (Key State Variables) | ไฟล์ที่เกี่ยวข้อง (Related Files) | หมายเหตุ (Notes) |
> | :--- | :--- | :--- | :--- | :--- |
> | *Example* Application | `AppRow` | `applications` | `dashboard/page.tsx` | ใช้ D1 เป็นหลัก |
> | *Example* User | `User` | `currentUser` | `auth.ts` | - |

---

## 🔄 Phase 2: The Workflow Protocol (Standard Operating Procedure)

ห้ามเริ่มเขียน Code จนกว่าจะทำตามขั้นตอน **"Inception"** ครบถ้วน เพื่อกันการหลุด Scope

### 🟢 Step 1: Inception (รับงานใหม่)
1.  **Analyze**: อ่าน Requirement และผลกระทบ
2.  **Update `task.md`**: สร้าง Task ใหม่ **[T-XXX]** พร้อมรายละเอียด (Concept & Implementation Details)
3.  **Update `spec.md`**: หากมี Feature ใหม่ ให้เพิ่ม **[F-XXX]** ลงในตาราง
4.  **Update `traceability.md`**: เพิ่มแถวใหม่ ผูก **F-XXX** กับ **T-XXX** ไว้ (สถานะ Planned)

### 🟡 Step 2: Execution (ลงมือทำ)
1.  **Checklist Driven**: ทำงานทีละ Sub-task ใน `task.md`
2.  **Environment Sync**: หากมีการติดตั้ง Library ใหม่ หรือเพิ่ม Env Var **ต้องอัปเดต `instruction.md` และ `.env.example` ทันที** (อย่ารอจบงาน เดี๋ยวลืม)

### ⚠️ Error Logging & Prevention Protocol (Critical Rule)
หากเกิด Error หรือ Bug ที่ต้องแก้ Code เดิม หรือต้องปรับเปลี่ยน Logic ระหว่างทำ Task:
1.  **ห้ามแก้เงียบๆเด็ดขาด**
2.  ต้องบันทึกลง `SpecKit/task.md` ในข้อย่อยของ Task นั้นๆ ทันที เพื่อเป็นบทเรียนให้ AI ในอนาคต
3.  **Strict Format**:
    ```markdown
    - [x] Sub-task Name
        - **Error Encountered**: [ระบุ Error Message หรืออาการที่เจอ]
        - **Root Cause**: [สาเหตุที่แท้จริง (ไม่ใช่แค่อาการ)]
        - **Solution**: [วิธีแก้ไขที่ทำลงไป]
        - **Prevention**: [ข้อเน้นย้ำ/จุดระวัง เพื่อไม่ให้เกิดซ้ำอีกในอนาคต] <--- ต้องมี!
    ```

### 🔴 Step 3: Closure (ปิดงาน)
1.  **Mark Complete**: ติ๊กถูก [x] ใน `task.md`
2.  **Link Code**: เอา Path ของไฟล์ที่สร้าง/แก้ไข ไปเติมในช่อง `Key Code Files` ใน `traceability.md`
3.  **Status Update**: เปลี่ยนสถานะใน `traceability.md` เป็น **Done**

---

## 🤝 Phase 3: Handoff Protocol (การรับ-ส่งไม้ต่อ)
เพื่อให้ AI Session ถัดไปทำงานต่อได้ทันทีโดยไม่ต้องเริ่มเล่าใหม่

### **คำสั่งแรกสำหรับ Chat ใหม่ (The Rehydration Prompt)**
> "สวัสดี เรามาทำโปรเจกต์นี้ต่อกัน
> งานทั้งหมดถูก Document ไว้ในโฟลเดอร์ `SpecKit/` แล้ว
>
> **ภารกิจของคุณ (Mission):**
> 1.  **Context Loading**: อ่านไฟล์ตามลำดับนี้:
>     -   `SpecKit/instruction.md` (Check Tech Stack & **Active Constraints**)
>     -   `SpecKit/spec.md` (Understand Flow & Features)
>     -   `SpecKit/traceability.md` (Check Data Map & Feature Map)
>     -   `SpecKit/task.md` (Check Status, **Verification Steps**, และ **Error Logs** ย้อนหลัง)
>
> 2.  **Status Check**: สรุปให้ฟังสั้นๆ ว่า:
>     -   งานล่าสุดที่เพิ่งเสร็จคืออะไร?
>     -   Task ต่อไปที่ต้องทำคืออะไร? (ตาม `task.md`)
>
> 3.  **Action**: รอคำสั่งยืนยันจากเราก่อนเริ่มเขียน Code (ห้ามคิดไปเอง)"

---

## ✅ Summary Checklist for New Project Setup
- [ ] Folder `SpecKit/` created
- [ ] `spec.md` initialized
- [ ] `instruction.md` initialized (Included Known Issues section)
- [ ] `task.md` initialized (Included Decision Logs & Verification Steps)
- [ ] `traceability.md` initialized (Feature & Data Matrices)
